import 'package:flutter/material.dart';
import 'widget_tab.dart';

class WorkoutsTab extends WidgetTab {

  WorkoutsTab(String title) : super(title, mainColor: Colors.red);


  @override
  Widget build(BuildContext context) {

    return Container(
      
    );
  }
}